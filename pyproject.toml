[project]
name = "hash-it"
version = "0.0.1"
requires-python = ">=3.10"
dependencies = [
    "crcmod==1.7",
    "docopt==0.6.2",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "packaging==26.2",
    "parameterized==0.9.0",
    "pytest==9.0.3",
]
